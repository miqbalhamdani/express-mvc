<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>

    <style>
      body {
        display: flex;
        align-items: center;
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
        text-align: center;
        height: calc(100vh - 200px);
      }

      .form-signin {
        width: 100%;
        max-width: 330px;
        padding: 15px;
        margin: auto;
      }
    </style>
  </head>
  <body>
    <%- body %>
  </body>
</html>
